/**
 * Placeholder for FontPickerField
 * TODO: Implement in Phase 2-4 migration
 */

interface Props {
  compositeKey: string;
}

function FontPickerField({ compositeKey }: Props) {
  // Placeholder - will be implemented during migration
  return (
    <div data-field-placeholder="FontPickerField" data-composite-key={compositeKey}>
      [FontPickerField placeholder]
    </div>
  );
}

export default FontPickerField;
